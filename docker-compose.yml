services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: zombly
      POSTGRES_PASSWORD: zombly
      POSTGRES_DB: zombly
    ports:
      - '5432:5432'
    volumes:
      - zombly-pgdata:/var/lib/postgresql/data

volumes:
  zombly-pgdata:
